# Distributed implementation status

This document prevents scaffolding from being mistaken for completed production
functionality.

| Area | Implemented in this change | Still required for production parity |
|---|---|---|
| Contracts | Protocol v2 with an explicit accepted v1 N-1 window; versioned Run/Task/Schedule/Target/Capability/Sync/Snapshot/Approval/Desktop schemas; generated OpenAPI 3.1 and JSON Schema 2020-12 release/runtime artifacts; Axum route-drift, `$ref`, operation-ID and breaking-change CI gates | Broader historical corpus once further protocol versions have shipped |
| Durable runs | PostgreSQL queue, thread serialization, immutable target, events/SSE, atomic durable event cursors, enforced 90-day terminal-run event retention, atomic user-message/run creation with retry idempotency, persisted assistant results, leases, interruption, task versions, DST-safe schedules/catch-up, approvals, inputs, checkpoints, push, authenticated remote terminals, real N-1 migration/restore acceptance, killed-worker/executor-disconnect chaos acceptance with pre-dispatch unsafe checkpoints, and an extended four-process/500-run contention acceptance proving exactly-once dispatch and cross-run result/message isolation | Cross-OS worker soak matrix |
| Server execution | Shared Rust runtime, external OpenAI-compatible providers, signed Docker sandboxes, MCP/artifact flow, Playwright browser tools, policy-controlled capabilities, and adversarial runner tests | Complete cross-platform tool parity and sustained GUI acceptance |
| Isolation | Dedicated socket-owning runner, HMAC/replay protection, image allowlist, non-root/read-only/cap-drop/quotas, explicit fail-closed Seccomp, a repository AppArmor profile plus startup probe, absent Docker socket, cross-run volume isolation, and tested filtered egress denial for private/host/metadata networks | Production kernel/Docker/AppArmor compatibility matrix |
| Linux GUI/Office | Pinned Chromium/Playwright and LibreOffice/Xvfb/Openbox image; authenticated stream/takeover/audit; OOXML/UNO processing and review artifacts | Visual regression expansion and sustained GUI load acceptance |
| Personal desktop | Durable SQLite daemon, local Ollama/vLLM-compatible calls, authenticated per-user IPC, Tauri bridge, background recovery and server connector; the outbound device agent delegates server-originated personal runs to that same daemon with exact Run identity, bound-private/snapshot workspaces, encrypted per-run model settings and relayed events/checkpoints/approvals/input/artifacts; ordinary changed files are captured under the `.coworkignore` boundary, uploaded as resumable/deduplicated encrypted chunks and atomically linked as a review-only result project version without automatic apply; transient device-WebSocket loss recovers the same unexpired lease while stable source IDs make event/checkpoint/intervention/artifact replay idempotent, including an acceptance where the daemon finishes offline; hash-verified Windows/Linux bundle, private self-provisioned token/device identity, cross-process single-instance lock, stable per-user installation, immediate detached start, Windows HKCU and Linux systemd/XDG login start; SIGTERM/console/session-end shutdown handling with authenticated IPC shutdown, worker cancellation and persisted interruption checkpoints, including a real stalled-run `WM_QUERYENDSESSION` lifecycle acceptance; owner-only personal Windows and Linux GUI streaming with compressed RFB frames, input/clipboard relay, server-and-agent-enforced capability checks and `off`/per-session-confirmation/unattended policy, plus an Xvfb Linux acceptance | Real signed installer/login-start and real-profile rollback acceptance on every supported OS |
| Windows pool | Revocable credentials, outbound WebSocket leases/events/results, one-run isolation, interactive COM/GUI Office automation, desktop stream/control, macro blocking and cleanup health gate | Hardware/VM matrix and Microsoft Office channel compatibility acceptance |
| Web | Isolated compile-time web entry with no loaded Tauri runtime; canonical same-origin control plane; durable thread conversations, remote runs, schedules, approvals/input, terminal, GUI takeover, artifacts and account security; rotating Secure/HttpOnly/SameSite browser session with HTTPS login/reload/replay/logout acceptance; manual CI verifies the remote-only boundary and WCAG 2.1 AA under current Chromium, Firefox and WebKit, exercises 25 unique cookie rotations in the standard profile and 250 in the extended profile before replay-family revocation and logout | Older/mobile browser compatibility matrix |
| Android | Tauri 2 shell with Keystore-encrypted run/event/conversation cache, biometric/PIN lock, offline outbox, FCM, attachments, run/terminal/GUI control and native system-browser PKCE/passkeys; manual CI builds a release-mode arm64 APK, validates its package payload and publishes a checksummed seven-day unsigned acceptance artifact | Credentialed signed APK and physical-device release acceptance |
| Auth/RBAC | Bootstrap, invitations, Argon2id, TOTP/recovery, domain-bound passkeys, Discovery/JWKS OIDC with PKCE/nonce, system-browser native PKCE, native-vault and web-HttpOnly token rotation/replay revocation, owner-scoped signed-in device inventory and revocation with cross-user isolation/audit, bounded 90-day expired/revoked session and refresh-replay retention, default-deny admin content access, 24-hour support grants, executor credentials and team/project/pool RBAC | Full identity-provider matrix and broader adversarial authorization acceptance |
| Sync/storage | Resumable content-addressed chunks, dedupe, AES-256-GCM envelope encryption, `.coworkignore` boundary, secret warnings, retention/GC, a device-session-bound metadata Push/Pull/SSE API with idempotent operation IDs, optimistic revisions, tombstones, monotone inbox cursors and consistent paginated bootstrap snapshots; private synced projects, their thread membership and messages are transactionally materialized into the canonical control-plane tables with provenance collision protection and out-of-order convergence; server-created private projects, threads and durable user/assistant messages are projected atomically back into the device feed and support a bidirectional rename roundtrip; the persistent personal Device Agent drains the daemon Outbox and applies the Inbox without WebView lifetime coupling, using owner-bound executor credentials, stable operation IDs, durable per-server cursors, echo-free remote writes and persisted explicit conflict resolutions; versions, three-way review, quotas, a logical 20-GiB acceptance, network-fault chaos acceptance for partial-write avoidance/resume/reservations/retryable GC, path/virtual-hosted SigV4 with optional signed session tokens and a provider-neutral external acceptance test; sustained 8-client/90-second pressure acceptance with 1,804 encrypted roundtrips, concurrent GC, zero leaked chunks/nonnegative refcounts and bounded memory; digest-pinned current/N-1 MinIO compatibility rows locally green and scheduled weekly | Conflict-resolution UI and complete reverse projection for future canonical update/delete endpoints; credentialed non-MinIO provider rows (AWS S3/R2/other) require operator-owned test buckets and protected CI credentials |
| Deployment | One Caddy host port, internal API/worker/Postgres/MinIO/runner, healthchecks, NPM guide, default-deny CSP/HSTS/cross-origin browser headers, a TLS Nginx/NPM-compatible-to-Caddy E2E for web/SSE/binary WebSocket/20-MiB upload, versioned GHCR release images with SBOM/provenance, consistent backup/restore/automatic rollback scripts, redacted audited support bundles, local aggregate metrics, a green 100-user/25-run load test, and standard/extended browser plus multi-worker soak profiles | Physical NPM release/TLS matrix and repeated restore drills |

Until the right-hand column is completed and the security acceptance suite is
green, deploy this foundation only in a trusted development or pilot network.
