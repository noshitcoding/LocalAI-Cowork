[package]
name = "tauri-plugin-mobile-secure"
version = "0.3.0"
description = "Open Cowork AndroidKeyStore-backed secret storage"
edition = "2021"
rust-version = "1.85"
license = "Apache-2.0"
links = "tauri-plugin-mobile-secure"
build = "build.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
tauri = { version = "2.10.3", default-features = false }
thiserror = "2"

[build-dependencies]
tauri-plugin = { version = "2.5.4", features = ["build"] }
